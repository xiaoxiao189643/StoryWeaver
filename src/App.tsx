// ============================================================
// src/App.tsx —— 根 React 组件
// ============================================================
import { UIOverlay } from "./react/components/UIOverlay";

export function App() {
  return <UIOverlay />;
}